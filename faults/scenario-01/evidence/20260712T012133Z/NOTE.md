Superseded by `../20260712T012421Z/`, which added Part 2 (transient `-error-rate=0.5` test) to
the same script. Part 1's numbers here are identical to Part 1 in the superseded-by run (the mock
engine's failure decisions and the seeded workload are both deterministic, so a same-script rerun
reproduces exactly) — kept per the evidence rule "invalid/superseded runs are noted, never
deleted," not because these specific numbers are wrong.
